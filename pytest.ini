[pytest]
markers =
    live: hits a real, paid/quota-limited external service. Excluded by
          default (see addopts below) -- run explicitly with
          `pytest -m live tests/integration` when you specifically want to
          spend real provider quota/credit verifying credentials.
addopts = -m "not live"
