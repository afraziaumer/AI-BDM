aiofiles==25.1.0
aiohappyeyeballs==2.6.2
aiohttp==3.14.1
aioresponses==0.7.8
aiosignal==1.4.0
annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.14.0
attrs==26.1.0
beautifulsoup4==4.15.0
certifi==2026.6.17
charset-normalizer==3.4.7
click==8.4.1
distro==1.9.0
dnspython==2.8.0
fastapi==0.138.0
filelock==3.29.4
frozenlist==1.8.0
greenlet==3.5.2
groq==1.5.0
h11==0.16.0
httpcore==1.0.9
httpretty==1.1.4
httpx==0.28.1
huepy==1.2.1
idna==3.18
multidict==6.7.1
numpy==2.5.0
packaging==26.2
playwright==1.60.0
propcache==0.5.2
pydantic==2.13.4
pydantic_core==2.46.4
pyee==13.0.1
python-dotenv==1.2.2
phonenumbers==9.0.33
rank-bm25==0.2.2
requests==2.34.2
requests-file==3.0.1
sentence-transformers==5.6.0
# IMPORTANT (Windows): after installing, run
#   pip uninstall -y lxml pyarrow datasets
# lxml is NOT required — every BeautifulSoup() call in this project explicitly
# uses "html.parser" (never request the lxml parser again). pyarrow/datasets
# are pulled in transitively by sentence-transformers' optional training-
# pipeline submodule, never used here (only .encode() is used). Confirmed via
# a live faulthandler crash trace: loading pyarrow's C extension in the same
# process as this project's other native deps causes an immediate,
# untrappable segfault (access violation) on import — not a Python exception,
# so no amount of try/except in this codebase can guard against it. Keeping
# either package installed WILL reintroduce that crash the moment Steps 3+/6
# (torch/embeddings) run in the same process as Step 2 (the scraper).
sniffio==1.3.1
soupsieve==2.8.4
starlette==1.3.1
langdetect==1.0.9
tldextract==5.3.1
typing-inspection==0.4.2
typing_extensions==4.15.0
urllib3==2.7.0
uvicorn==0.49.0
# wappalyzer 2.0.1 has three real bugs in the installed package itself, all
# manually patched in this environment's site-packages (NOT tracked by this
# repo -- a fresh `pip install` will silently reintroduce all three; reapply
# by hand or via tech_stack.py's own doc-comments if you reinstall):
#   1. wappalyzer/Wappalyzer.py:58 -- BeautifulSoup(self.html, 'lxml') hardcodes
#      the lxml parser; changed to 'html.parser' (lxml conflicts with this
#      project's other native deps -- see the note on lxml/pyarrow below).
#   2. wappalyzer/core/config.py -- opens technologies.json/categories.json/
#      groups.json without encoding='utf-8'; on Windows this defaults to
#      cp1252 and crashes on the files' real UTF-8 content. Added
#      encoding='utf-8' to all three `open()` calls.
#   3. wappalyzer/data/technologies.json -- the Symfony fingerprint's `html`
#      regex uses JS's `[^]` ("match any character") idiom, which Python's re
#      module doesn't support and mis-parses as an unbalanced-paren error,
#      silently disabling Symfony detection. Replaced `[^]` with the
#      Python-compatible equivalent `[\s\S]`.
wappalyzer==2.0.1
yarl==1.24.2
