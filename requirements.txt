aiofiles==25.1.0
aiohappyeyeballs==2.6.2
aiohttp==3.14.1
aioresponses==0.7.8
aiosignal==1.4.0
annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.14.0
attrs==26.1.0
beautifulsoup4==4.15.0
boto3==1.43.64
certifi==2026.6.17
charset-normalizer==3.4.7
click==8.4.1
distro==1.9.0
dnspython==2.8.0
fastapi==0.138.0
filelock==3.29.4
frozenlist==1.8.0
greenlet==3.5.2
groq==1.5.0
h11==0.16.0
httpcore==1.0.9
httpretty==1.1.4
httpx==0.28.1
huepy==1.2.1
idna==3.18
multidict==6.7.1
numpy==2.5.0
packaging==26.2
playwright==1.60.0
propcache==0.5.2
pydantic==2.13.4
pydantic_core==2.46.4
pyee==13.0.1
pytest==9.1.1
iniconfig==2.3.0
pluggy==1.6.0
python-dotenv==1.2.2
phonenumbers==9.0.33
rank-bm25==0.2.2
regex==2026.5.9
requests==2.34.2
requests-file==3.0.1
sentence-transformers==5.6.0
# IMPORTANT (Windows): after installing, run
#   pip uninstall -y lxml pyarrow datasets
# lxml is NOT required — every BeautifulSoup() call in this project explicitly
# uses "html.parser" (never request the lxml parser again). pyarrow/datasets
# are pulled in transitively by sentence-transformers' optional training-
# pipeline submodule, never used here (only .encode() is used). Confirmed via
# a live faulthandler crash trace: loading pyarrow's C extension in the same
# process as this project's other native deps causes an immediate,
# untrappable segfault (access violation) on import — not a Python exception,
# so no amount of try/except in this codebase can guard against it. Keeping
# either package installed WILL reintroduce that crash the moment Steps 3+/6
# (torch/embeddings) run in the same process as Step 2 (the scraper).
sniffio==1.3.1
soupsieve==2.8.4
starlette==1.3.1
langdetect==1.0.9
tldextract==5.3.1
typing-inspection==0.4.2
typing_extensions==4.15.0
urllib3==2.7.0
uvicorn==0.49.0
# wappalyzer is intentionally NOT installed via pip -- do not add
# "wappalyzer==2.0.1" here. Confirmed live (2026-08-06): PyPI serves
# different, incompatible code under that exact same version string over
# time (no Wappalyzer.py, no .latest() classmethod in the current build,
# vs. what this project needs) -- the version pin does not guarantee
# reproducible package contents. tech_stack.py instead uses a vendored,
# pre-patched copy committed at vendor/wappalyzer/ (see that directory's
# own README and docs/KNOWN_LIMITATIONS.md), which sidesteps this problem
# permanently regardless of what PyPI serves in the future.
yarl==1.24.2
