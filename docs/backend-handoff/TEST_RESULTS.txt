AI-BDM — Test Results
======================

Offline + mocked suite (`pytest -q`)
-------------------------------------
Commit:  690f939
Date:    2026-08-06
Command: pytest -q
Result:  101 passed, 4 deselected, 2 warnings

Deselected tests are the 4 live-provider tests in tests/integration/,
excluded by default via pytest.ini's `addopts = -m "not live"`.

Suite breakdown:
  tests/smoke/       14 tests — core module imports, Stage 8 accuracy-check
                     functions against fixtures. No network, no API keys.
  tests/unit/        unit tests — request validation, effective-limit
                     resolution, discovery-gate scoring, homepage-selection
                     rollup, Windows path handling, Wappalyzer patch
                     verification, job_translation.target_to_query(),
                     job_contracts.classify_error()'s error taxonomy.
  tests/mocked/      review-escalation logic, transient-failure-vs-genuine-
                     empty caching, Google Maps matching hierarchy, and the
                     full async job lifecycle (job_runner.py) — submit,
                     progress, cancellation, duplicate/unknown job_id,
                     unexpected-exception handling, status mapping — all
                     with phase1_pipeline.run_pipeline replaced by a fake.
  tests/contract/    9 tests — realistic request/progress/result/error
                     examples validate against job_contracts.py's Pydantic
                     models, plus a drift check that the committed
                     docs/backend-handoff/schemas/*.json files still match
                     what the current models actually generate.

Live-provider suite (`pytest -m live tests/integration`)
-----------------------------------------------------------
Last run against real credentials earlier in this same handoff pass (not
re-run for this file -- would spend real quota with no live-provider code
change to justify it):
  4 passed — Groq key validity, ScrapingBee key/quota, Apify token
  validity, Serper key validity (the Serper check spends 1 real search
  credit, by design — see the test file for the documented cost).

Re-run directly when needed:
  pytest -m live tests/integration

Known failing tests
---------------------
None, on either a genuinely fresh install or this development machine, as
of the commit above.

What this does NOT cover
---------------------------
- A dedicated test for "untrusted scraped page text cannot alter LLM tool
  configuration/paths/credentials/scope" (Laravel guide Section 4.9's
  Security row) — see docs/backend-handoff/KNOWN_LIMITATIONS.md.
- Stage 3 (route/page selection), Stage 4 (final reasoning), and Stage 6
  (RAG embedding/retrieval) have no dedicated unit/mocked test — see
  docs/backend-handoff/PIPELINE_STAGES.md for exactly what each stage's
  coverage is.
- The job runner's tests replace phase1_pipeline.run_pipeline with a fake
  -- they prove job_runner.py's own logic (job store, progress/cancel
  wiring, result assembly, status mapping) is correct, not that the real
  pipeline's progress_cb/cancel_check calls fire at exactly the right
  moments end-to-end against a real query. That would require either a
  live run or a much deeper mock of phase1_pipeline's internals; neither
  exists yet.
- No durable-job-store failure-mode test (e.g. "process restarts mid-job")
  -- there's nothing to test, since no durable store exists (see
  docs/backend-handoff/JOB_EXECUTION.md).
