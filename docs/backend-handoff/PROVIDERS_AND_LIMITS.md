# External Providers and Limits

One consolidated table, matching the Laravel Backend Construction Handoff Guide's Section 4.8 format. This pulls together information that otherwise lives scattered across `README.md`, `docs/ARCHITECTURE.md`, `docs/RUNBOOK.md`, and `.env.example` — those documents remain the deeper reference for *why* each behavior exists; this one is the flat lookup table.

| Provider / library | Operation | Variable name(s) | Request shape | Quota | Timeout | Retry | Cache |
|---|---|---|---|---|---|---|---|
| Groq (LLM) | Query planning, clarification, route-planning fallback, final reasoning, relevance safety-net, moderation | `groq_llm_apikey1` / `GROQ_API_KEY` (required) | One prompt + one worked example per call, via `LLM_planner.call_llm(task=TaskType.X)` | Governed by the Groq account's own plan (tokens-per-minute); not separately throttled by this codebase | 30s per call (planning/reasoning tasks) | Up to the model's configured retry count, then falls back to `qwen/qwen3.6-27b` (universal fallback for both tiers) | Not cached — every call is live; the *plan itself* isn't cached, though its downstream effects (a committed business) are |
| Serper.dev | Discovery search (Step 2), Google Maps/Places (Step 7), review-platform `site:` discovery searches | `serper` / `SERPER_API_KEY` (required) | Search query string + optional location/country params | Governed by the Serper.dev plan's own quota; not separately throttled here | Not separately configured — inherits the underlying HTTP client's default | A failed request is never cached as a genuine empty result — retried on the next run (see `docs/DATA_CONTRACTS.md`'s cache-correctness table) | Places/Maps match result cached at `storage/<domain>/reviews/google_maps.json`; discovery search results are not cached (re-run re-searches) |
| ScrapingBee (variable named `zenrows` for historical reasons — **not** a ZenRows key) | Premium/JS-render fetch tier, used when a native fetch is blocked (Cloudflare challenge, captcha, bot detection) | `zenrows` / `ZENROWS_API_KEY` (recommended, not required) | One target URL per call | Monthly call quota per the ScrapingBee plan — exhaustion degrades this tier pipeline-wide until reset/upgrade (`"Monthly API calls limit reached"` in logs) | Not separately configured — inherits the underlying HTTP client's default | Per-domain circuit breaker opens after 2 consecutive failures for that domain, falling back to native-fetch-only for its remaining pages rather than retrying indefinitely | Not cached at the request level — a re-crawled page re-fetches |
| Apify | Google Maps review-text harvesting (`compass/Google-Maps-Reviews-Scraper` actor) and Reddit mention harvesting (`trudax/reddit-scraper-lite` actor) | `apify` / `APIFY_API_TOKEN` (optional) | Actor run config (target URL/query + review count target, with an escalation loop that doubles the ask up to a ceiling if fewer reviews-with-text come back than requested) | Governed by the Apify actor's own run configuration/quota | Not separately configured — inherits the Apify client's default | Escalation loop stops early when the provider returns fewer raw items than requested (pool exhausted) or the ceiling is hit; see `tests/mocked/test_review_escalation.py` | Result cached at `storage/<domain>/reviews/<platform>.json`; failures are never cached (see cache-correctness table) |
| GitHub API | Social-presence enrichment (`github_enrichment.py`) | `GITHUB_TOKEN` / `github_token` (optional) | Standard GitHub REST API calls | Governed by GitHub's own published API rate limits | Not separately configured | Not separately configured beyond the HTTP client's default | Result stored under `storage/<domain>/social_profiles.json` |
| YouTube Data API | Channel lookup (`youtube_enrichment.py`) | `YOUTUBE_API_KEY` / `youtube_api_key` (optional) | Standard YouTube Data API calls | Governed by the YouTube Data API's own published quota | Not separately configured | Not separately configured | Result stored under `storage/<domain>/social_profiles.json` |
| Cloudflare R2 | Remote sync of each committed business's local storage folder | `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_BUCKET_NAME`, `R2_ACCOUNT_ID` (or `R2_ENDPOINT`) — all optional; missing credentials silently disable sync (never an error) | Whole-folder atomic sync via `storage_sync.py`'s `R2StorageProvider` | Governed by the R2/S3-compatible plan | Inherits boto3's default | Incremental — skips unchanged files via a per-domain manifest | N/A (this *is* the durable copy) |
| Wappalyzer / tech detection | Technology profile (Stage 5) | None (vendored, not pip-installed — see `vendor/wappalyzer/README.md`) | Local fingerprint match against already-fetched HTML/headers + one DNS lookup + one small `robots.txt` fetch | N/A — local computation, only two small bounded network extras | Bounded by the one DNS lookup and one `robots.txt` fetch | Each of the 4 engines wrapped independently — one failing doesn't break the others | Result written to `storage/<domain>/tech_stack.json` |
| `sentence-transformers` / Chroma | Evidence embedding + retrieval (Stage 6) | None — local model, no API key | N/A — local computation | N/A | N/A (bounded by local compute) | N/A | Chroma vector store at `rag/.chroma/`; rebuild by deleting that directory |

## Notes on quota/rate-limit behavior generally

- Every provider integration in this codebase distinguishes a **genuinely empty response** (safe to cache) from a **request failure** (network/timeout/non-200/rate-limit — never cached, always retried on the next run). This is tested directly in `tests/mocked/test_cache_correctness.py` and is the single most load-bearing reliability property of the whole pipeline — see `docs/DATA_CONTRACTS.md`.
- None of these providers currently map onto the Laravel guide's 8-class error taxonomy (`validation`/`authentication`/`quota`/`rate_limit`/`transient_provider`/`blocked_source`/`data_quality`/`internal`) in a structured way — failures surface as log lines and free-text error strings today. This is explicitly out of scope for the current handoff pass (tracked as a 4.3–4.6 job-contract dependency) — see `KNOWN_LIMITATIONS.md` in this folder.
